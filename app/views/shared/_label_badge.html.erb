<%# locals: (label:) %>
<span class="inline-flex max-w-full items-center truncate rounded px-2 py-0.5 text-[10px] font-bold uppercase tracking-wide shadow-sm <%= label.badge_classes %>">
  <%= label.name %>
</span>
