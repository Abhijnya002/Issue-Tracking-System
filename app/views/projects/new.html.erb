<% content_for :title, "New project" %>

<div class="mx-auto max-w-xl">
  <h1 class="text-2xl font-semibold">New project</h1>
  <p class="mt-1 text-sm text-slate-600">Keys appear on issues (for example, CRM-12). Use 2–10 letters or numbers.</p>

  <%= render "form", project: @project %>
</div>
